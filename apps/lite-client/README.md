# W.O.L.F.E Lite client

The Lite client is a thin entry point over the shared media client. It keeps the approved bedroom-profile rules while reusing the same UI, catalogue and player modules.

Until the final client split, `Launch WOLFE Lite.bat` starts the shared client with `?mode=lite`.
