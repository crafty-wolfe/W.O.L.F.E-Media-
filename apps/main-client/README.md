# W.O.L.F.E Main client

The Main client is a thin entry point over the shared media client. It enables household-wide profile selection and administrator settings.

Until the final client split, `Launch WOLFE Main.bat` starts the shared client with `?mode=main`.
